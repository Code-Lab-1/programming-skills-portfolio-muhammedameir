prompt = "\nWhat will the toppings be on your pizza?"
prompt += "\nEnter 'done' when you are done selecting: "

while True:
    topping = input(prompt)
    if topping != 'quit':
        print(f"  I'll add {topping} to your pizza.")
    else:
        break
